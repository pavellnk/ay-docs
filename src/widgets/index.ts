/**
 * Widgets Layer
 * 
 * Contains composite UI components that combine multiple features
 * and shared components. Widgets are self-contained and reusable
 * across different pages.
 */

export { FeaturesSection } from './features-section';

